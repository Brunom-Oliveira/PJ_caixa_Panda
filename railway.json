{
  "buildCommand": "cd backend && npm install && npx prisma generate && npm run build",
  "startCommand": "export DATABASE_URL=\"file:$(pwd)/backend/banco.db\" && npx prisma db push --schema=backend/prisma/schema.prisma && node backend/dist/index.js"
}
